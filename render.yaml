services:
  - type: worker
    name: swedish-learning-bot
    env: python
    buildCommand: pip install -r requirements.txt && python scripts/download_saldo.py && python scripts/saldo_parser.py
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
